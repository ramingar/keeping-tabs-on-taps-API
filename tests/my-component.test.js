import test from 'tape';

test('-------- Component: ...', (assert) => {
    const message = '...';
    const expected = 1;
    const actual = 1;

    assert.equal(actual, expected, message);

    assert.end();
});
